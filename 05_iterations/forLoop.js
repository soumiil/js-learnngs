let array = [2,3,4,5,6,"Soumil"];

console.log(`Length of array is ${array.length}`);

for (let index = 0; index < array.length; index++) {
    // if(array[index] == 5) {
    //     console.log(array[index]);
    //     break;
    // }
    const element = array[index];
    console.log(element);
}